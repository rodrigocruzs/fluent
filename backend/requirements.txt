# Single source of truth for backend Python deps lives in api/requirements.txt,
# because that is the file Vercel installs (see vercel.json requirementsFile).
# Local dev installs the exact same set via this include so the two can never
# drift again (a past drift here took the whole /api surface down in prod).
-r ../api/requirements.txt
